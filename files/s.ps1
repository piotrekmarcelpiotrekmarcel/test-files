# Staged payload for T1059 test #27 (HTTPS download cradle + TLS cert validation bypass)
"$(Get-Date -Format o) | s.ps1 executed on $(hostname) as $(whoami)" | Out-File -Append C:\purpletest\evidence\cradle_s.log
ipconfig /all
